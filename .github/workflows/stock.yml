name: AI Stock System

on:
workflow_dispatch:

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: Send Telegram Message
    run: |
      curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
      -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
      -d text="AI选股系统运行成功"
```
